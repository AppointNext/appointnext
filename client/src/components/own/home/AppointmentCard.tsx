"use client";

interface AppointmentCardProps {
  image: string;
  title: string;
  topic: string;
  daysLeft: number;
}

export default function AppoinmentCard({
  image,
  title,
  topic,
  daysLeft,
}: AppointmentCardProps) {
  return <div></div>;
}
